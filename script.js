function getComputerChoice() {
  return Math.floor(Math.random() * 3);
};

function getPlayerChoice() {
  // Request string input from player
  // Make string case insensitive
  // Check that string is within choice
  // Map string to integer 0-2
  // Return numeric value
};

function evaluateChoices() {
  
}

function playRound() {
  // Call function that gets player choice
  // Call function that gets computer choice
  // Call function that compares the two values
};

